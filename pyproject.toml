[project]
name = "testing-task"
version = "0.3"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpath = ["tests"]
log_cli = true
log_cli_level = "INFO"
log_cli_format = "%(asctime)s | %(name)s | %(levelname)s | %(message)s"
